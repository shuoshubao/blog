# asynchronous iterators

```
async function process(array) {
    for await (let i of array) {
        doSomething(i);
    }
}
```

# Promise

-   Promise.prototype.finally()

# Object

-   Rest/Spread
